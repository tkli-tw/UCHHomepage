
//-------------------------------------------------------
// 定義一個 最簡單的 simple brick 類別
// brick 需要一個可以放置的 holder，放它，
// 這個 holder 的型態 可能要另外再定義，否則，不曉得要怎麼放，
// 這個 holder 必需要有一個 "place" 的方法，以便於在這裡 叫用，來將 brick 放進去。
// location = {'row':1, 'col':1}   從  (0,0) 開始
// 有些 holder 不需要 location.
//-------------------------------------------------------
function SimpleBrick(holder, location){
	// 測試一下 函數呼叫，參數傳遞的情況。
	// 三個參數，叫用時，按順序傳入，若是只傳一個，就傳到 holder，傳兩個，就是 holder, row ，剩下的就是 undefined。
	//console.log(holder, location);
	
}




